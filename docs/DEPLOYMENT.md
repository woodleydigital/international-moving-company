# IMC hosting and repository handoff

## Source repository

The user selected `woodleydigital/international-moving-company` as the GitHub repository for the site. Keep complete source, assets, the npm lockfile and project guidelines in that repository. Generated `out/`, dependency folders and credentials are excluded.

## Vercel configuration

- Repository root: `/`.
- Framework: Next.js.
- Install: `npm ci`.
- Build: `npm run build`.
- Static output: `out/`, generated by the existing `output: "export"` setting.
- Intended public domain: `internationalmoving.company`.

The committed `vercel.json` records these build settings. It does not add or verify the custom domain, configure DNS, or grant Vercel access to GitHub. Verify the account-side project connection and a successful deployment before changing DNS.

## Connection status recorded 14 September 2026

The connected Vercel team was returned as `matthew-1234s-projects`. A direct preview deployment request returned a deployment link, but subsequent project and deployment reads returned not found, and project listing remained empty. That request is unverified and must not be described as a successful Vercel deployment. The user subsequently reported connecting the GitHub repository; confirm that link through live Vercel project/deployment data after the repository is populated.

The established Sites preview is a separate deployment. Its hosting metadata is retained for continuity. Updating GitHub must not be described as updating that preview, or as connecting the public domain.

## Launch boundaries

Preserve `noindex, nofollow` and the disabled enquiry submission while the site remains a preview. Verify hosting access controls separately. The form delivery service, Google Places configuration, approved legal/operator details and public indexing review remain launch work. Use only the exact DNS records supplied by the final Vercel project; preserve unrelated email and verification records.

No redirect or site-move configuration from MoverFocus.com or internationalmoving.services is part of this project.
