export function PlanningVisual({kind}:{kind:'packing'|'storage'|'delivery'}){
const data={packing:{title:'Follow each item from assessment to collection',steps:[['Identify','Item, quantity and condition'],['Prepare','Agreed wrapping, packing or dismantling'],['Record','Label linked to the collection inventory']],caption:'The item description, preparation instructions and inventory record should refer to the same belongings. Confirm who completes each action.'},storage:{title:'Where storage can sit in your moving plan',steps:[['Origin storage','Hold before international departure'],['International transport','Follow the agreed shipment route'],['Destination storage','Hold before the home is ready']],caption:'Storage may be needed at origin, destination or neither. This shows possible positions in the journey, not a requirement to store twice or a promise of availability.'},delivery:{title:'Three checks before the final handoff',steps:[['Ready to schedule','Release status and delivery arrangements'],['Ready to receive','Access, recipient and room plan'],['Ready to record','Inventory checks and outstanding issues']],caption:'Release, the delivery appointment and completion checks are separate steps. Confirm the applicable arrangements with your move coordinator.'}}[kind];
return <figure className="moving-graphic planning-visual" aria-label={data.title}><div className="graphic-title">{data.title}</div><ol>{data.steps.map(([title,description],i)=><li key={title}><span>{String(i+1).padStart(2,'0')}</span><strong>{title}</strong><p>{description}</p></li>)}</ol><figcaption>{data.caption}</figcaption></figure>}
